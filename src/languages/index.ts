import en from './en';
import es from './es';

export const dictionaryList: any = {
    en,
    es
};

export const languageOptions: any = [
    { id: 'en', text: 'English' },
    { id: 'es', text: 'Spanish' }
];